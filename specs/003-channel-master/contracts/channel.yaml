openapi: 3.0.3
info:
  title: Channel Master API
  version: 0.1.0
  description: >
    REST contracts for channel master data, authorization lifecycle, KPI, alerts,
    and strategy configuration inside PowerX plugin `/v1/channels/**`.
servers:
  - url: https://example.com/_p/{pluginId}/api/v1
    variables:
      pluginId:
        default: com.powerx.plugin.ecommerce
security:
  - bearerAuth: []
  - X-PowerX-Tenant: []
paths:
  /channels:
    get:
      summary: List channels with filters
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - in: query
          name: platform
          schema:
            type: string
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/ChannelStatus'
        - in: query
          name: owner_uuid
          schema:
            type: string
            format: uuid
        - in: query
          name: region
          schema:
            type: string
        - in: query
          name: min_health
          schema:
            type: integer
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Paginated channels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelListResponse'
    post:
      summary: Create channel draft
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChannelUpsertRequest'
      responses:
        '201':
          description: Channel draft created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelDetail'
  /channels/{channelId}:
    get:
      summary: Get channel detail + KPI
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '200':
          description: Channel detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelDetail'
    patch:
      summary: Update channel master data
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChannelUpsertRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelDetail'
  /channels/{channelId}/submit:
    post:
      summary: Submit channel for approval
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '202':
          description: Approval request accepted
  /channels/{channelId}/approval:
    post:
      summary: Approve or reject channel
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision]
              properties:
                decision:
                  type: string
                  enum: [approve, reject]
                reason:
                  type: string
      responses:
        '200':
          description: Status updated
  /channels/{channelId}/credentials:
    get:
      summary: Retrieve credential metadata
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '200':
          description: Credential list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Credential'
    post:
      summary: Create or refresh credential
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CredentialUpsertRequest'
      responses:
        '201':
          description: Credential stored/tested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Credential'
  /channels/{channelId}/credentials/test:
    post:
      summary: Trigger credential test
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '202':
          description: Test scheduled
  /channels/{channelId}/alerts:
    get:
      summary: List alerts for a channel
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '200':
          description: Alerts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Alert'
  /channels/{channelId}/alerts/{alertId}:
    patch:
      summary: Update alert status or assignee
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
        - in: path
          name: alertId
          schema:
            type: string
            format: uuid
          required: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: [acknowledged, resolved]
                assignee_uuid:
                  type: string
                  format: uuid
                note:
                  type: string
      responses:
        '200':
          description: Alert updated
  /channels/{channelId}/sync:
    post:
      summary: Trigger data sync job
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '202':
          description: Sync scheduled
  /channels/{channelId}/tasks:
    post:
      summary: Attach channel to an existing remediation task
      parameters:
        - $ref: '#/components/parameters/TenantHeader'
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [task_id]
              properties:
                task_id:
                  type: string
                note:
                  type: string
      responses:
        '200':
          description: Task linked

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    X-PowerX-Tenant:
      type: apiKey
      in: header
      name: X-PowerX-Tenant
  parameters:
    TenantHeader:
      in: header
      name: X-PowerX-Tenant
      schema:
        type: string
      required: true
    ChannelId:
      in: path
      name: channelId
      schema:
        type: string
        format: uuid
      required: true
  schemas:
    ChannelStatus:
      type: string
      enum: [draft, pending_review, rejected, unauthorized, authorized, disabled]
    ChannelSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        platform:
          type: string
        store_id:
          type: string
        name:
          type: string
        region:
          type: string
        status:
          $ref: '#/components/schemas/ChannelStatus'
        tags:
          type: array
          items:
            type: string
        owner_uuid:
          type: string
          format: uuid
        gmv:
          type: number
        orders:
          type: integer
        health_score:
          type: integer
        last_sync_at:
          type: string
          format: date-time
    ChannelListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ChannelSummary'
        next_cursor:
          type: string
          nullable: true
    ChannelUpsertRequest:
      type: object
      required:
        - platform
        - store_id
        - name
        - region
        - owner_uuid
        - contact
      properties:
        platform:
          type: string
        store_id:
          type: string
        name:
          type: string
        domain:
          type: string
        region:
          type: string
        owner_uuid:
          type: string
        approver_uuid:
          type: string
        contact:
          type: object
          required: [name, phone, email]
          properties:
            name:
              type: string
            phone:
              type: string
            email:
              type: string
              format: email
        strategy:
          type: object
          properties:
            pricebook_id:
              type: string
            inventory_strategy_id:
              type: string
            logistics_strategy_id:
              type: string
            cs_sla_id:
              type: string
            fee_rate:
              type: number
              format: float
            settlement_cycle:
              type: string
            payment_terms:
              type: string
        notes:
          type: string
    ChannelDetail:
      allOf:
        - $ref: '#/components/schemas/ChannelSummary'
        - type: object
          properties:
            contact:
              $ref: '#/components/schemas/Contact'
            strategy:
              $ref: '#/components/schemas/ChannelStrategy'
            metrics:
              type: array
              items:
                $ref: '#/components/schemas/ChannelMetric'
            alerts:
              type: array
              items:
                $ref: '#/components/schemas/Alert'
            audit_logs:
              type: array
              items:
                $ref: '#/components/schemas/AuditLog'
    Contact:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        email:
          type: string
    ChannelStrategy:
      type: object
      properties:
        pricebook_id:
          type: string
        inventory_strategy_id:
          type: string
        logistics_strategy_id:
          type: string
        cs_sla_id:
          type: string
        fee_rate:
          type: number
        settlement_cycle:
          type: string
        payment_terms:
          type: string
    ChannelMetric:
      type: object
      properties:
        window:
          type: string
          enum: [d1, d7, d30]
        gmv:
          type: number
        orders:
          type: integer
        gmv_growth_rate:
          type: number
        inventory_coverage:
          type: number
        error_rate:
          type: number
        sync_success_rate:
          type: number
        health_score:
          type: integer
        source_timestamp:
          type: string
          format: date-time
    Credential:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum: [oauth, api_key, offline]
        scope:
          type: array
          items:
            type: string
        expires_at:
          type: string
          format: date-time
        status:
          type: string
          enum: [valid, expiring, expired, revoked, test_failed]
        last_refreshed_at:
          type: string
          format: date-time
        test_result:
          type: object
    CredentialUpsertRequest:
      type: object
      required:
        - type
        - payload
      properties:
        type:
          type: string
        payload:
          type: object
          description: Token/code/credential blob depending on type
        scope:
          type: array
          items:
            type: string
        expires_at:
          type: string
          format: date-time
    Alert:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
        severity:
          type: string
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [open, acknowledged, resolved]
        triggered_at:
          type: string
          format: date-time
        resolved_at:
          type: string
          format: date-time
        assignee_uuid:
          type: string
          format: uuid
        task_id:
          type: string
    AuditLog:
      type: object
      properties:
        id:
          type: string
        action:
          type: string
        actor_uuid:
          type: string
        actor_role:
          type: string
        context:
          type: object
        created_at:
          type: string
          format: date-time
